<!DOCTYPE html>
<html>
<head>
    <title>Приветствие</title>
</head>
<body>
    <!-- Blade-синтаксис для вывода переменной -->
    <h1>Привет, {{ $name }}!</h1>
    
    <!-- Простой пример использования условий в Blade -->
    @if(strlen($name) > 5)
        <p>У вас длинное имя!</p>
    @else
        <p>У вас короткое имя!</p>
    @endif
    
    <p>Текущее время: {{ date('H:i:s') }}</p>
</body>
</html>